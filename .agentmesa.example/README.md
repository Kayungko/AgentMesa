# .agentmesa Example

This folder shows the intended local project state directory structure.

```txt
.agentmesa/
  meetings/
  tasks/
  messages/
  artifacts/
  logs/
  locks/
  state.sqlite
```

The real `.agentmesa/` directory should normally be local project state and may be partially ignored depending on team policy.
