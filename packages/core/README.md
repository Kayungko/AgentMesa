# @agentmesa/core

Local state and task bus for AgentMesa.

Planned responsibilities:

- Read/write `.agentmesa/` state.
- Manage tasks, messages, artifacts, logs, and locks.
- Provide APIs for CLI, MCP server, runner, and future dashboard.
