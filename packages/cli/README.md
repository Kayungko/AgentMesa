# @agentmesa/cli

Command line interface for AgentMesa.

Early command sketch:

```bash
mesa init
mesa task create "Implement feature"
mesa task list
mesa task show T-0001
mesa handoff T-0001 --from claude --to codex
mesa status T-0001
```
